export const emailSelector = v => v.state.email;
export const usernameSelector = v => v.state.username;
export const firstNameSelector = v => v.state.firstName;
export const lastNameSelector = v => v.state.lastName;
